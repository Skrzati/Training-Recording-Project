spring.application.name=SavingWorkoutsApp

# Po??czenie z baz? danych
spring.datasource.url=jdbc:postgresql://localhost:5432/workout_tracker
spring.datasource.username=postgres
spring.datasource.password=admin

# Sterownik (opcjonalne w nowszych wersjach, ale warto mie? dla pewno?ci)
spring.datasource.driver-class-name=org.postgresql.Driver

# Konfiguracja Hibernate (JPA)
# 'update' oznacza, ?e Hibernate sam utworzy/zaktualizuje tabele na podstawie Twoich klas Javy
spring.jpa.hibernate.ddl-auto=update

# Wy?wietlanie zapyta? SQL w konsoli (przydatne do nauki i debugowania)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true